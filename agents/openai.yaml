interface:
  display_name: "GNOME Wayland Computer Use"
  short_description: "Cua-native GNOME desktop control and observation"
  default_prompt: "Use $gnome-wayland-computer-use with Cua as the control authority to operate this Ubuntu GNOME Wayland desktop and verify the requested result."

policy:
  allow_implicit_invocation: true
